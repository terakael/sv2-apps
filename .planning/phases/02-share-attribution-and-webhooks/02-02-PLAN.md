---
phase: 02-share-attribution-and-webhooks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pool-apps/pool/src/lib/channel_manager/mod.rs
  - pool-apps/pool/src/lib/channel_manager/template_distribution_message_handler.rs
autonomous: true

must_haves:
  truths:
    - "Job IDs are mapped to user IDs when coinbase is updated"
    - "Mapping persists across coinbase switches to handle lagging shares"
    - "Stale mappings are cleaned up on SetNewPrevHash to prevent memory leak"
  artifacts:
    - path: "pool-apps/pool/src/lib/channel_manager/mod.rs"
      provides: "job_to_user HashMap in ChannelManagerData"
      contains: "job_to_user: HashMap<u32, String>"
    - path: "pool-apps/pool/src/lib/channel_manager/mod.rs"
      provides: "Job mapping population in update_coinbase_and_broadcast"
      pattern: "job_to_user\\.insert"
    - path: "pool-apps/pool/src/lib/channel_manager/template_distribution_message_handler.rs"
      provides: "Cleanup logic in handle_set_new_prev_hash"
      pattern: "job_to_user\\.clear"
  key_links:
    - from: "pool-apps/pool/src/lib/channel_manager/mod.rs"
      to: "job_to_user HashMap"
      via: "Populated BEFORE messages broadcast"
      pattern: "job_to_user\\.insert.*forward"
    - from: "pool-apps/pool/src/lib/channel_manager/template_distribution_message_handler.rs"
      to: "job_to_user HashMap"
      via: "Cleared on SetNewPrevHash"
      pattern: "handle_set_new_prev_hash.*job_to_user"
---

<objective>
Add job-to-user attribution mapping to ChannelManager with proper population during coinbase updates and cleanup on blockchain state changes.

Purpose: Enables correct user attribution for shares (WEBHOOK-02, WEBHOOK-06, WEBHOOK-07) and prevents memory leaks through stale mapping cleanup (CONC-03).
Output: ChannelManagerData with job_to_user HashMap, population logic in update_coinbase_and_broadcast, and cleanup in handle_set_new_prev_hash.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-share-attribution-and-webhooks/02-RESEARCH.md
@.planning/phases/01-/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add job_to_user HashMap to ChannelManagerData</name>
  <files>
pool-apps/pool/src/lib/channel_manager/mod.rs
  </files>
  <action>
Add job_to_user field to ChannelManagerData struct (around line 61-82):

```rust
/// Maps job_id → user_id for share attribution (WEBHOOK-06)
/// Populated when update_coinbase_and_broadcast creates jobs
/// Consumed when handle_submit_shares validates shares
/// Cleaned up on SetNewPrevHash to prevent memory leak (CONC-03)
pub(crate) job_to_user: HashMap<u32, String>,
```

Import HashMap at top of file if not already present:
```rust
use std::collections::HashMap;
```

Initialize field in ChannelManager::new (wherever ChannelManagerData is constructed):
```rust
job_to_user: HashMap::new(),
```

Follow research Pattern 1 "Job-to-User Mapping in ChannelManagerData" from 02-RESEARCH.md lines 67-104 and Code Example lines 488-502.
  </action>
  <verify>
cargo check --manifest-path pool-apps/pool/Cargo.toml
grep "job_to_user: HashMap" pool-apps/pool/src/lib/channel_manager/mod.rs
Verify field exists in ChannelManagerData struct.
  </verify>
  <done>
job_to_user HashMap field added to ChannelManagerData with documentation. HashMap imported. Field initialized in constructor. Compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate job_to_user mapping in update_coinbase_and_broadcast</name>
  <files>
pool-apps/pool/src/lib/channel_manager/mod.rs
  </files>
  <action>
Modify update_coinbase_and_broadcast method (lines 727-877) to populate job_to_user mapping.

CRITICAL ORDERING (Pitfall 1 - lines 327-335 in research):
Store mapping BEFORE broadcasting messages. Must be atomic within super_safe_lock.

Location: Inside super_safe_lock after job creation, before message broadcast (around line 794-863).

For each downstream that creates jobs:
1. Extract job_id from standard channels: standard_channel.get_active_job().get_job_id()
2. Extract job_id from extended channels if applicable
3. Insert into job_to_user: `channel_manager_data.job_to_user.insert(job_id, user_id.clone())`

Place mapping inserts BEFORE the messages.extend() call (around line 863), ensuring mapping exists before messages forwarded.

Follow research Pattern 1 "Populating Job Mapping During Coinbase Update" from 02-RESEARCH.md lines 505-552.

WARNING: Do NOT insert after super_safe_lock ends. Must be inside lock to be atomic with message generation.
  </action>
  <verify>
cargo check --manifest-path pool-apps/pool/Cargo.toml
grep -A5 "job_to_user.insert" pool-apps/pool/src/lib/channel_manager/mod.rs
Verify inserts occur BEFORE message broadcast (before forward() calls).
  </verify>
  <done>
job_to_user mapping populated during update_coinbase_and_broadcast. Mapping inserted BEFORE messages broadcast. Both standard and extended channel job IDs tracked. Atomic within super_safe_lock. Compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add cleanup in handle_set_new_prev_hash</name>
  <files>
pool-apps/pool/src/lib/channel_manager/template_distribution_message_handler.rs
  </files>
  <action>
Modify handle_set_new_prev_hash method (lines 157-240) to clean up stale job mappings.

Add cleanup logic at the START of the method, BEFORE processing new prevhash (around line 165):

```rust
// CONC-03: Clean up stale job mappings to prevent memory leak
self.channel_manager_data.super_safe_lock(|data| {
    let jobs_before = data.job_to_user.len();

    // Strategy: Clear all mappings on new prevhash
    // Jobs become stale when blockchain advances
    // Next coinbase update will repopulate for new template
    data.job_to_user.clear();

    info!(
        "Cleaned up {} stale job mappings on SetNewPrevHash",
        jobs_before
    );
});
```

This implements CONC-03 requirement: "Pool cleans up stale job-to-user mappings on SetNewPrevHash (prevents memory leak)".

Follow research Pattern 3 "SetNewPrevHash Cleanup Hook" from 02-RESEARCH.md lines 170-204 and Code Example lines 555-589.

NOTE: Strategy is simple clear-all. Jobs from previous templates become stale on blockchain advancement. Next coinbase update will repopulate. This addresses Pitfall 3 (memory leak) from lines 347-356.
  </action>
  <verify>
cargo check --manifest-path pool-apps/pool/Cargo.toml
grep -B2 -A5 "job_to_user.clear" pool-apps/pool/src/lib/channel_manager/template_distribution_message_handler.rs
Verify clear() called early in handle_set_new_prev_hash.
  </verify>
  <done>
Cleanup logic added to handle_set_new_prev_hash. job_to_user.clear() called at start of method. Logging shows cleanup count. Prevents unbounded HashMap growth. Compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. Run `cargo check --manifest-path pool-apps/pool/Cargo.toml` - should compile without errors
2. Verify job_to_user field exists in ChannelManagerData struct
3. Check update_coinbase_and_broadcast inserts mappings (grep "job_to_user.insert")
4. Verify inserts occur BEFORE message broadcast (check ordering vs forward() calls)
5. Confirm handle_set_new_prev_hash calls job_to_user.clear()
6. Check cleanup occurs early in method (before template processing)
</verification>

<success_criteria>
- job_to_user HashMap added to ChannelManagerData with documentation
- Mapping populated in update_coinbase_and_broadcast for all channel types
- Mapping inserted BEFORE messages broadcast (atomic ordering)
- Cleanup implemented in handle_set_new_prev_hash (prevents memory leak)
- All compilation successful with no errors
- WEBHOOK-06, WEBHOOK-07, CONC-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-share-attribution-and-webhooks/02-02-SUMMARY.md` following the standard summary template.
</output>
