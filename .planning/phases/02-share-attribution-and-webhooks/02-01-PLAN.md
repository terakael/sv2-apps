---
phase: 02-share-attribution-and-webhooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pool-apps/pool/Cargo.toml
  - pool-apps/pool/src/lib/webhook/mod.rs
  - pool-apps/pool/src/lib/webhook/types.rs
  - pool-apps/pool/src/lib/webhook/client.rs
  - pool-apps/pool/src/lib/mod.rs
  - pool-apps/pool/src/lib/config.rs
autonomous: true

must_haves:
  truths:
    - "WebhookClient can send share notifications without blocking caller"
    - "Webhook failures are logged but don't crash pool"
    - "Webhook payload includes all required fields from WEBHOOK-02 through WEBHOOK-05"
  artifacts:
    - path: "pool-apps/pool/src/lib/webhook/mod.rs"
      provides: "Webhook module exports"
      min_lines: 10
    - path: "pool-apps/pool/src/lib/webhook/types.rs"
      provides: "ShareWebhookPayload struct with all required fields"
      exports: ["ShareWebhookPayload"]
    - path: "pool-apps/pool/src/lib/webhook/client.rs"
      provides: "WebhookClient with send_share_notification method"
      exports: ["WebhookClient"]
  key_links:
    - from: "pool-apps/pool/src/lib/webhook/client.rs"
      to: "reqwest::Client"
      via: "HTTP client for webhook delivery"
      pattern: "reqwest::Client"
    - from: "pool-apps/pool/src/lib/webhook/client.rs"
      to: "tokio::spawn"
      via: "Fire-and-forget background task"
      pattern: "tokio::spawn"
---

<objective>
Create webhook notification infrastructure with reqwest-based HTTP client and fire-and-forget delivery pattern.

Purpose: Establishes the foundation for share notifications (WEBHOOK-01), ensuring webhook delivery never blocks pool operations (WEBHOOK-08) and failures are logged properly (WEBHOOK-09).
Output: Complete webhook module with WebhookClient, ShareWebhookPayload type, and connection pooling configuration.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-share-attribution-and-webhooks/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reqwest dependency and create webhook module structure</name>
  <files>
pool-apps/pool/Cargo.toml
pool-apps/pool/src/lib/webhook/mod.rs
pool-apps/pool/src/lib/mod.rs
  </files>
  <action>
Add reqwest and chrono dependencies to Cargo.toml:
```toml
[dependencies]
reqwest = { version = "0.12", features = ["json"] }
chrono = "0.4"
```

Create webhook module at `pool-apps/pool/src/lib/webhook/mod.rs`:
- Module documentation explaining webhook notification system
- Re-export WebhookClient and ShareWebhookPayload
- Sub-module declarations for types and client

Add webhook module declaration to `pool-apps/pool/src/lib/mod.rs`:
```rust
pub mod webhook;
```

Follow research Pattern 2 (fire-and-forget) architecture from 02-RESEARCH.md lines 107-164.
  </action>
  <verify>
cargo check --manifest-path pool-apps/pool/Cargo.toml
Verify webhook module compiles without errors.
  </verify>
  <done>
Webhook module structure exists with proper exports. reqwest and chrono dependencies added to Cargo.toml. Module declared in lib/mod.rs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ShareWebhookPayload type</name>
  <files>
pool-apps/pool/src/lib/webhook/types.rs
  </files>
  <action>
Create `pool-apps/pool/src/lib/webhook/types.rs` implementing ShareWebhookPayload struct.

Required fields per WEBHOOK-02 through WEBHOOK-05:
- user_id: String (WEBHOOK-02 - from job-to-user mapping)
- share_hash: String (WEBHOOK-03 - hex format)
- difficulty: f64 (WEBHOOK-03)
- job_id: u32 (WEBHOOK-03)
- timestamp: i64 (WEBHOOK-03 - Unix timestamp)
- is_block: bool (WEBHOOK-04 - true for BlockFound, false for Valid)
- channel_id: u32 (WEBHOOK-05)
- downstream_id: usize (WEBHOOK-05)
- sequence_number: u32 (WEBHOOK-05)

Use #[derive(Serialize, Clone, Debug)] for serialization and debugging.
Add documentation comments explaining each field's purpose.

Follow research Code Example "Webhook Payload Type" from 02-RESEARCH.md lines 383-416.
  </action>
  <verify>
cargo check --manifest-path pool-apps/pool/Cargo.toml
Verify ShareWebhookPayload compiles with all required fields and derives.
  </verify>
  <done>
ShareWebhookPayload struct exists with all 9 required fields. Type implements Serialize, Clone, Debug. Documentation explains field purposes. Compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement WebhookClient with fire-and-forget pattern</name>
  <files>
pool-apps/pool/src/lib/webhook/client.rs
pool-apps/pool/src/lib/config.rs
  </files>
  <action>
Create `pool-apps/pool/src/lib/webhook/client.rs` implementing WebhookClient.

WebhookClient structure:
- Clone-able struct holding reqwest::Client and webhook_url
- Client configured with 5-second timeout and connection pooling (max 10 idle per host)
- new(webhook_url: String) constructor

send_share_notification method:
- Takes ShareWebhookPayload by value
- Uses #[instrument] macro with user_id, job_id, is_block in span fields
- Clones client and URL for tokio::spawn
- Spawns background task (fire-and-forget - WEBHOOK-08)
- Posts JSON payload to webhook URL
- Logs success at info level
- Logs errors at error level with details (WEBHOOK-09)
- Returns immediately without awaiting HTTP response

Add webhook_url field to PoolConfig in `pool-apps/pool/src/lib/config.rs`:
- Add webhook_url: String field to struct
- Update new() constructor to accept webhook_url parameter
- Update Debug impl if necessary

Follow research Pattern 2 and Code Example "WebhookClient with reqwest" from 02-RESEARCH.md lines 107-164 and 419-485.

CRITICAL: Follow Pitfall 2 guidance - never .await in validation path, always tokio::spawn (lines 337-345).
  </action>
  <verify>
cargo check --manifest-path pool-apps/pool/Cargo.toml
cargo test --manifest-path pool-apps/pool/Cargo.toml webhook::client
Verify WebhookClient compiles, new() and send_share_notification methods exist.
  </verify>
  <done>
WebhookClient exists with send_share_notification using tokio::spawn. Method returns immediately without blocking. Errors logged via tracing. webhook_url added to PoolConfig. All compilation successful.
  </done>
</task>

</tasks>

<verification>
1. Run `cargo check --manifest-path pool-apps/pool/Cargo.toml` - should compile without errors
2. Verify webhook module exports WebhookClient and ShareWebhookPayload
3. Check WebhookClient::new accepts webhook_url parameter
4. Verify send_share_notification uses tokio::spawn (grep for "spawn" in client.rs)
5. Confirm reqwest and chrono in Cargo.toml dependencies
</verification>

<success_criteria>
- Webhook module compiles successfully with reqwest dependency
- ShareWebhookPayload has all 9 required fields per WEBHOOK-02 through WEBHOOK-05
- WebhookClient uses fire-and-forget pattern (tokio::spawn, no .await blocking)
- Webhook failures logged via tracing (error level)
- PoolConfig extended with webhook_url field
- No compilation errors in webhook module
</success_criteria>

<output>
After completion, create `.planning/phases/02-share-attribution-and-webhooks/02-01-SUMMARY.md` following the standard summary template.
</output>
