---
phase: 01-api-foundation-and-template-switching
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - integration-tests/tests/test_coinbase_merkle_validity.rs
  - integration-tests/Cargo.toml
autonomous: true

must_haves:
  truths:
    - "Test connects miner to pool and receives initial mining job"
    - "Test modifies coinbase outputs in ChannelManager state"
    - "Test submits valid share with modified coinbase"
    - "Pool accepts share without merkle path validation errors"
  artifacts:
    - path: "integration-tests/tests/test_coinbase_merkle_validity.rs"
      provides: "Integration test validating merkle path reuse after coinbase modification"
      min_lines: 100
  key_links:
    - from: "integration-tests/tests/test_coinbase_merkle_validity.rs"
      to: "pool-apps/pool ChannelManager"
      via: "Direct state modification of coinbase_outputs"
      pattern: "channel_manager_data.*coinbase_outputs"
    - from: "test share submission"
      to: "pool share validation"
      via: "SubmitSharesStandard message"
      pattern: "SubmitSharesStandard"
---

<objective>
Validate the core feasibility assumption: merkle paths remain valid when coinbase outputs change. This is a BLOCKER - if this test fails, the entire dynamic coinbase switching approach requires rearchitecture.

Purpose: Empirically confirm that shares are accepted after coinbase modification, proving merkle path computation is independent of coinbase content (as theory suggests). Research identified this as critical uncertainty (RESEARCH.md lines 466-469).

Output: Passing integration test demonstrating share acceptance after coinbase switch, giving confidence to proceed with API implementation.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/.planning/PROJECT.md
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/.planning/ROADMAP.md
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/.planning/STATE.md
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/.planning/phases/01-/01-RESEARCH.md

# Existing integration test patterns
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/integration-tests/tests/
</context>

<tasks>

<task type="auto">
  <name>Create merkle path validation integration test</name>
  <files>
integration-tests/tests/test_coinbase_merkle_validity.rs
integration-tests/Cargo.toml
  </files>
  <action>
Create integration test following existing test patterns in integration-tests/tests/ directory.

**Test structure:**
1. **Setup:** Start regtest pool and connect test miner (reuse patterns from existing integration tests)
2. **Receive initial job:** Wait for NewMiningJob message, extract job_id and other parameters
3. **Modify coinbase:** Directly access pool's ChannelManager and change coinbase_outputs (simulate what API will do):
   - Get lock on channel_manager_data
   - Replace coinbase_outputs with new address scriptPubKey
   - Release lock
4. **Submit valid share:** Mine a valid share (or use pre-computed valid share data) and submit via SubmitSharesStandard
5. **Assert acceptance:** Verify pool responds with success, NOT with ShareValidationError::InvalidMerkleRoot

**Key validation points:**
- Pool accepts share after coinbase modification (proves merkle path validity)
- No "Invalid merkle root" errors in pool logs
- Share validation succeeds using modified coinbase

**Critical to test:** This validates TMPL-05 (merkle path theory). Research identifies this as feasibility blocker (RESEARCH.md Pitfall 2, lines 279-288).

If test fails: STOP and document findings. This invalidates the approach and requires architectural redesign.

Add any needed test dependencies to integration-tests/Cargo.toml (likely none - existing test infrastructure should suffice).
  </action>
  <verify>
Run `cargo nextest run --manifest-path integration-tests/Cargo.toml test_coinbase_merkle_validity` and confirm:
1. Test passes without panics or assertion failures
2. No "Invalid merkle root" errors in output
3. Pool accepts share after coinbase modification
  </verify>
  <done>
Test exists, passes consistently, proves shares accepted after coinbase switch, validates core feasibility assumption.
  </done>
</task>

<task type="auto">
  <name>Document test findings and update risk status</name>
  <files>
integration-tests/tests/test_coinbase_merkle_validity.rs
  </files>
  <action>
Add comprehensive documentation to test file explaining:

**Header comment block:**
- Purpose: Validates merkle path reuse after coinbase modification (TMPL-05)
- Why critical: Core feasibility assumption for dynamic coinbase switching
- What it proves: Merkle paths remain valid when coinbase outputs change
- References: Links to RESEARCH.md Pitfall 2, REQUIREMENTS.md TMPL-05

**Inline comments:**
- Document each test phase (setup, modify, submit, verify)
- Explain why direct state modification simulates HTTP API behavior
- Note implications if test were to fail

**Test output:**
- Use tracing or println to show: original coinbase, modified coinbase, share acceptance result
- Make it clear when the critical moment happens (share validation after modification)

This documentation ensures future developers understand why this test exists and what it validates.
  </action>
  <verify>
Read test file and confirm documentation clearly explains purpose, methodology, and implications. Comments should make test self-explanatory to someone unfamiliar with the project.
  </verify>
  <done>
Test file has comprehensive documentation explaining feasibility validation, critical moments are annotated, purpose is clear from reading the code.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Integration test validating merkle path reuse after coinbase modification. This test empirically proves the core feasibility assumption for dynamic coinbase switching.
  </what-built>
  <how-to-verify>
1. Review test results: Check that test passed without errors
2. Review test code: Confirm test actually modifies coinbase and submits share
3. Check pool logs: Verify no "Invalid merkle root" or share rejection errors
4. Understand implications: If test passed, merkle path reuse is validated and we can proceed with confidence. If test failed (unlikely based on codebase analysis), the entire approach needs rearchitecture.

Expected outcome: Test passes, proving merkle paths remain valid after coinbase changes.
  </how-to-verify>
  <resume-signal>
Type "validated" to confirm merkle path reuse works and proceed with API implementation.
Or describe any issues/failures for investigation.
  </resume-signal>
</task>

</tasks>

<verification>
1. Test exists: `ls integration-tests/tests/test_coinbase_merkle_validity.rs`
2. Test passes: `cargo nextest run test_coinbase_merkle_validity` succeeds
3. Documentation: Test file has clear comments explaining purpose and methodology
4. Validation: Human confirms test results prove feasibility assumption
</verification>

<success_criteria>
- Integration test compiles and runs successfully
- Test modifies coinbase outputs in ChannelManager state
- Test submits valid share after coinbase modification
- Pool accepts share without merkle path errors
- Test documentation explains why this validation is critical
- Human verification confirms feasibility assumption validated
</success_criteria>

<output>
After completion, create `.planning/phases/01-/01-02-SUMMARY.md`
</output>
