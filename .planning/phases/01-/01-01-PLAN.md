---
phase: 01-api-foundation-and-template-switching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pool-apps/pool/src/lib/http_api/mod.rs
  - pool-apps/pool/src/lib/http_api/types.rs
  - pool-apps/pool/src/lib/lib.rs
autonomous: true

must_haves:
  truths:
    - "HTTP API module compiles without errors"
    - "Request type validates user_id constraints (max 128 chars, alphanumeric + underscore/hyphen)"
    - "Response type serializes to JSON with success and message fields"
  artifacts:
    - path: "pool-apps/pool/src/lib/http_api/mod.rs"
      provides: "HTTP API module declaration and re-exports"
      min_lines: 10
    - path: "pool-apps/pool/src/lib/http_api/types.rs"
      provides: "Request/response types with validation"
      min_lines: 40
  key_links:
    - from: "pool-apps/pool/src/lib/lib.rs"
      to: "pool-apps/pool/src/lib/http_api/mod.rs"
      via: "pub mod http_api declaration"
      pattern: "pub mod http_api"
    - from: "pool-apps/pool/src/lib/http_api/types.rs"
      to: "serde::{Deserialize, Serialize}"
      via: "derive macros for JSON handling"
      pattern: "#\\[derive\\(.*Deserialize.*\\)\\]"
---

<objective>
Create HTTP API module structure with request/response types and validation logic for coinbase switching endpoint.

Purpose: Establish type-safe foundation for HTTP API before implementing pool integration. This allows parallel development of API structure and ChannelManager logic.

Output: Compilable http_api module with validated types ready for handler implementation.
</objective>

<execution_context>
@/home/dan/.claude/get-shit-done/workflows/execute-plan.md
@/home/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/.planning/PROJECT.md
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/.planning/ROADMAP.md
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/.planning/STATE.md
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/.planning/phases/01-/01-RESEARCH.md

# Existing pool structure for reference
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/pool-apps/pool/src/lib/mod.rs
@/home/dan/worktrees/personal/stratum/sv2-apps/dynamic-gsd/pool-apps/pool/src/lib/config.rs
</context>

<tasks>

<task type="auto">
  <name>Create HTTP API module structure</name>
  <files>
pool-apps/pool/src/lib/http_api/mod.rs
pool-apps/pool/src/lib/lib.rs
  </files>
  <action>
Create new directory `pool-apps/pool/src/lib/http_api/` and add module files:

**http_api/mod.rs:**
- Declare submodule: `pub mod types;` only (handlers.rs will be added in Plan 04)
- Re-export key types: `pub use types::{CoinbaseUpdateRequest, CoinbaseUpdateResponse};`
- Add module-level documentation explaining purpose (coinbase switching API)

**Update lib.rs:**
- Add `pub mod http_api;` after existing module declarations
- Keep alphabetical ordering with other modules (after `channel_manager`, before `io_task`)

**CRITICAL - Do NOT declare handlers module yet:** handlers.rs doesn't exist until Plan 04, so declaring it now would break cargo check. Plan 04 Task 1 will add the handlers module declaration when handlers.rs is actually created.

Follow existing pool module patterns from monitoring.rs and status.rs. Keep it minimal - just module structure, no implementations yet.
  </action>
  <verify>
Run `cargo check --manifest-path pool-apps/pool/Cargo.toml` to confirm module compiles without errors.
  </verify>
  <done>
http_api module declared in lib.rs, mod.rs exists with types submodule declaration only, cargo check passes.
  </done>
</task>

<task type="auto">
  <name>Implement request/response types with validation</name>
  <files>
pool-apps/pool/src/lib/http_api/types.rs
  </files>
  <action>
Create `http_api/types.rs` with request/response structures following RESEARCH.md patterns (lines 336-363):

**CoinbaseUpdateRequest:**
- Fields: `address: String`, `user_id: String`
- Derive: `Deserialize` (for axum Json extractor)
- Add validation method `validate(&self) -> Result<(), String>`:
  - user_id length <= 128 chars (API-03)
  - user_id chars alphanumeric + underscore/hyphen only (API-03)
  - Return descriptive error strings on validation failure

**CoinbaseUpdateResponse:**
- Fields: `success: bool`, `message: String`
- Derive: `Serialize` (for JSON response)
- Add constructor helpers: `success(message: impl Into<String>)`, `error(message: impl Into<String>)`

**Why no validation in address field:** Bitcoin address validation requires `bitcoin` crate integration, which happens in ChannelManager layer (Plan 03). This separation keeps types simple and testable.

Use standard serde derives, no custom serializers needed. Follow Rust naming conventions (snake_case for fields matching JSON).
  </action>
  <verify>
Run `cargo check --manifest-path pool-apps/pool/Cargo.toml` and confirm:
1. No compilation errors
2. Types are exported from http_api module
3. Validation logic compiles (create simple test if needed)
  </verify>
  <done>
types.rs exists with CoinbaseUpdateRequest (with validate()), CoinbaseUpdateResponse (with helpers), both derive correct traits, cargo check passes.
  </done>
</task>

<task type="auto">
  <name>Add unit tests for request validation</name>
  <files>
pool-apps/pool/src/lib/http_api/types.rs
  </files>
  <action>
Add test module at end of types.rs:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_valid_user_id() {
        // Test valid IDs: alphanumeric, underscore, hyphen
    }

    #[test]
    fn test_user_id_too_long() {
        // Test 129 chars -> validation error
    }

    #[test]
    fn test_user_id_invalid_chars() {
        // Test special chars like @, $, space -> validation error
    }

    #[test]
    fn test_response_constructors() {
        // Test success() and error() helpers
    }
}
```

Cover edge cases: empty user_id (valid), exactly 128 chars (valid), 129 chars (invalid), various invalid characters. Keep tests simple and focused on validation logic only.
  </action>
  <verify>
Run `cargo test --manifest-path pool-apps/pool/Cargo.toml --lib http_api::types` to confirm all validation tests pass.
  </verify>
  <done>
Test module exists with 4+ tests covering validation rules, all tests pass, validation behavior is verified.
  </done>
</task>

</tasks>

<verification>
1. Module structure: `ls pool-apps/pool/src/lib/http_api/` shows mod.rs and types.rs
2. Compilation: `cargo check --manifest-path pool-apps/pool/Cargo.toml` succeeds
3. Tests: `cargo test --manifest-path pool-apps/pool/Cargo.toml --lib http_api` all pass
4. Integration: `pub mod http_api` exists in lib.rs
</verification>

<success_criteria>
- http_api module compiles without warnings
- CoinbaseUpdateRequest validates user_id constraints per API-03
- CoinbaseUpdateResponse constructs success/error responses
- Unit tests verify validation edge cases
- Module follows existing pool patterns (monitoring.rs, status.rs)
</success_criteria>

<output>
After completion, create `.planning/phases/01-/01-01-SUMMARY.md`
</output>
